/**
 * 抽卡模块事件订阅登记
 *
 * GachaManager 构造器无事件订阅（抽卡仅经 char:get / items:use 事件对外派发），
 * 本文件按模块文件组织约定（见 AGENTS.md「文件命名」）占位，未来如需登记 gacha 事件订阅在此追加。
 */
export function registerGachaTriggers(): void {
  // 无构造器事件订阅，占位保持模块文件组织约定（见 AGENTS.md「文件命名」）一致
}
